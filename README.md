# GuruSpares React Frontend

React frontend for Guru Tex Spares.

## Scripts

- `npm install`
- `npm start`
- `npm run build`

The Django project serves the production build from `GuruSpares_frontend/build`.
